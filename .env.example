# Next.js talks to the ASP.NET Core API only.
NEXT_PUBLIC_API_URL=http://localhost:5000

# .NET API / PostgreSQL development values
ConnectionStrings__DefaultConnection=Host=localhost;Port=5432;Database=eleganza;Username=eleganza;Password=eleganza
ELEGANZA_DESIGN_CONNECTION=Host=localhost;Port=5432;Database=eleganza;Username=eleganza;Password=eleganza

# Vanex is configured per vendor in the application database; never commit a real token here.
