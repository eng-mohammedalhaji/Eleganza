# Eleganza project instructions

The application backend is ASP.NET Core on .NET 10 with PostgreSQL.
The Next.js frontend communicates with the backend through HTTP APIs.
Convex is not used by this project and must not be introduced.
